
put hidden code inside of blocks (to get assertion to highlight that block)
block dependent on assertions passed


style teacher edition metrics, button icons

// assertions shown as a row of colored blocks, highlighted when passed. 
// hint/solution as a colored dot below each.
// show a hand icon if help requested
// show an alert icon if inactive 
// show number of failed runs

favicon, pwa, installable, etc





db-based data
add challenges
update challenge from teacher version

add instructions, hint, solution. view them with button
track hints revealed, solutions viewed


challenge editor


...
 measure failed runs? (passing a new assertion is not a fail)